<snippet>
	<content><![CDATA[
${1:methodName}: enyo.inherit(function(sup) {
	return function($2) {
		$3
		sup.apply(this, arguments);
		$4
	}
})
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>enyoInherit</tabTrigger>
	<description>Enyo inherited method pattern</description>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.js</scope>
</snippet>
